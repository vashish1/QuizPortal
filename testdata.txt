leaderboard
Quizcompleted template
javascripts to pop up messages like incorrect username
Event added template
if secure cookie is null show a pop up telling sign in to continue


	fmt.Println("quiz chlra hai")
	u := findusername(w, r)
	user := database.Finddb(cl1, u)

	qu = Quiz{
		res:   w,
		req:   r,
		Index: 0,
	}
	switch r.Method {

	case "POST":
		{
			fmt.Println("anushiv testing 1")
			a := r.FormValue("eventname")
			if a != "" {
				qu.Eventname = a
				for _, b := range user.Score {
					if b.Event == a {
						level = b.Userlevel
					} else {
						level = 0

					}
				}
				qlist = database.Findfromquizdb(cl4, a)
				qu.Q = qlist[level]
				fmt.Print("Question is:", qu.Q)
				fmt.Println("stop 1")
				fmt.Println("stop2")
				ans = r.FormValue("answer")
				fmt.Println("answer", ans)
				fmt.Println("stop3")
				fmt.Printf("qu.Q.Answer%+v\n", qu.Q.Answer)
				if ans != "" {
					if ans == qu.Q.Answer {
						level++
						database.Updateuserscores(cl1, u, a, 10, level)
						fmt.Println("2level", level)
						fmt.Println("anushiv testing 2")
				     t, err := template.ParseFiles("C:/Users/yashi/go/src/QuizPortal/templates/quiz.html")
				     fmt.Println("anushiv testing 3")
				     if err != nil {
					log.Fatal("Could not parse template files:", err)
				}
				er := t.Execute(w, qu)
				if er != nil {
					log.Fatal("could not execute the files\n:", er)
				}
						// http.Redirect(w, r, "/quiz/", 302)
					} else {
						
						fmt.Println("ans does not match")
						http.Redirect(w, r, "/quiz/", 302)
					}
				} else {fmt.Println("anushiv testing 2")
				t, err := template.ParseFiles("C:/Users/yashi/go/src/QuizPortal/templates/quiz.html")
				fmt.Println("anushiv testing 3")
				if err != nil {
					log.Fatal("Could not parse template files:", err)
				}
				er := t.Execute(w, qu)
				if er != nil {
					log.Fatal("could not execute the files\n:", er)
				}
					fmt.Println("answer is null")
				}
				
				
				fmt.Println("stop 4")

			}
		}

	}